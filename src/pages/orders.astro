---
import '../styles/global.css';
import { getCurrentUser } from '../server/session';
const user = await getCurrentUser(Astro.request);
if (!user) return Astro.redirect(`/login?redirect=${encodeURIComponent('/orders')}`);
---
<html lang="en"><head><meta charset="utf-8" /><meta name="viewport" content="width=device-width" /><title>My orders — FreeCoffee.bio</title></head>
<body class="min-h-screen bg-muted/30"><main class="mx-auto max-w-3xl px-4 py-10"><header class="mb-8 flex items-center justify-between"><div><p class="font-mono text-xs uppercase tracking-widest text-primary">FreeCoffee.bio</p><h1 class="mt-2 text-3xl font-semibold">My orders</h1><p class="mt-2 text-muted-foreground">View your purchases and download digital products.</p></div><a class="text-sm text-primary hover:underline" href="/">Back home</a></header><div id="orders" class="grid gap-4"><p class="rounded-xl border bg-card p-6 text-muted-foreground">Loading orders…</p></div></main><script>
const root = document.querySelector('#orders');
const money = (amount, currency) => `${(amount / (currency === 'JPY' ? 1 : 100)).toFixed(currency === 'JPY' ? 0 : 2)} ${currency}`;
fetch('/api/orders').then(async response => { const data = await response.json(); if (!response.ok) throw new Error(data.error || 'Unable to load orders.'); return data.orders; }).then(orders => { if (!orders.length) { root.innerHTML = '<p class="rounded-xl border bg-card p-6 text-muted-foreground">You have no orders yet.</p>'; return; } root.replaceChildren(...orders.map(({ order, items, downloads }) => { const card = document.createElement('section'); card.className = 'rounded-xl border bg-card p-6 shadow-sm'; const names = items.map(item => `${item.name} × ${item.quantity}`).join(', '); card.innerHTML = `<div class="flex flex-wrap justify-between gap-2"><div><h2 class="font-semibold">${names || 'Order'}</h2><p class="mt-1 text-sm text-muted-foreground">Order ${order.id} · ${new Date(order.createdAt).toLocaleString()}</p></div><strong>${money(order.totalAmount, order.currency)}</strong></div><p class="mt-3 text-sm capitalize text-muted-foreground">Status: ${order.status}</p>`; if (downloads.length) { const list = document.createElement('div'); list.className = 'mt-4 grid gap-2 border-t pt-4'; downloads.forEach(download => { const link = document.createElement('a'); link.href = download.url; link.className = 'inline-flex w-fit rounded-lg bg-primary px-4 py-2 text-sm text-primary-foreground hover:opacity-90'; link.textContent = `Download (${download.count}/${download.max})`; list.append(link); }); card.append(list); } else if (order.status === 'paid') { const note = document.createElement('p'); note.className = 'mt-4 text-sm text-muted-foreground'; note.textContent = 'The download file is not available yet. Please contact the creator.'; card.append(note); } return card; })); }).catch(error => { root.innerHTML = `<p class="rounded-xl border border-destructive bg-card p-6 text-destructive">${error.message}</p>`; });
</script></body></html>
