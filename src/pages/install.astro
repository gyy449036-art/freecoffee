---
import '../styles/global.css';
import { hasRoot } from '../server/admin';

if (await hasRoot()) return new Response('Not Found', { status: 404 });
---
<html lang="en">
  <head><meta charset="utf-8" /><meta name="viewport" content="width=device-width" /><title>Install FreeCoffee.bio</title></head>
  <body class="min-h-screen bg-muted/30">
    <main class="mx-auto flex min-h-screen max-w-md items-center px-4 py-12">
      <section class="w-full rounded-xl border bg-card p-6 shadow-sm">
        <div class="mb-6 flex items-center gap-2 font-semibold"><span class="grid size-8 place-items-center rounded-lg bg-primary text-primary-foreground">☕</span> FreeCoffee<span class="text-primary">.bio</span></div>
        <p class="font-mono text-xs text-primary">FIRST DEPLOYMENT</p>
        <h1 class="mt-2 text-3xl font-semibold tracking-tight">Create your admin account</h1>
        <p class="mt-3 text-sm text-muted-foreground">This one-time setup creates the root administrator for this deployment. The install page closes after completion.</p>
        <form class="mt-7 space-y-4" method="post" action="/api/install">
          <label class="block text-sm font-medium">Name<input required name="name" maxlength="100" autocomplete="name" class="mt-2 h-11 w-full rounded-lg border bg-transparent px-3 outline-none focus:border-primary" /></label>
          <label class="block text-sm font-medium">Email<input required name="email" type="email" autocomplete="email" class="mt-2 h-11 w-full rounded-lg border bg-transparent px-3 outline-none focus:border-primary" /></label>
          <label class="block text-sm font-medium">Password<input required name="password" type="password" minlength="8" maxlength="128" autocomplete="new-password" class="mt-2 h-11 w-full rounded-lg border bg-transparent px-3 outline-none focus:border-primary" /><span class="mt-1 block text-xs text-muted-foreground">At least 8 characters.</span></label>
          <button class="h-11 w-full rounded-lg bg-primary px-4 text-sm font-medium text-primary-foreground hover:bg-primary/90" type="submit">Finish installation</button>
        </form>
      </section>
    </main>
  </body>
</html>
