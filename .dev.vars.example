# Copy to .dev.vars for local development; do not commit real values.

# Admin route segment; defaults to admin when empty.
ADMIN_PATH=admin

# Better Auth session signing secret. Generate with openssl rand -base64 32.
BETTER_AUTH_SECRET=

# Payment provider credentials are stored in D1 through the admin Payment settings page.
# This file intentionally has no payment provider variables.
