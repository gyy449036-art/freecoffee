# 复制为 .dev.vars 用于本地开发；不要提交真实值。
# Copy to .dev.vars for local development; do not commit real values.

# 后台管理入口路径；留空时默认为 admin。
ADMIN_PATH=admin

# Better Auth 会话签名密钥；请使用 openssl rand -base64 32 生成。
BETTER_AUTH_SECRET=
